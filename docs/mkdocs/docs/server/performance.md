# Performance

## Test environment

* CPU: Intel Core Ultra 5 250K Plus
* RAM: 24 GB (single-channel)
* Operating system: Windows 11
* Minecraft version: 26.2
* Mod version: 2.0.1 (DEV)
* World: standard survival world

## Test results

<div id="stress-test-chart" class="stress-chart" data-selected-metric="mspt">
  <div class="stress-chart__controls" role="group" aria-label="Select chart metric">
    <button type="button" data-metric="mspt" aria-pressed="true">MSPT</button>
    <button type="button" data-metric="duration" aria-pressed="false">Elapsed time breakdown</button>
  </div>
  <div class="stress-chart__canvas">
    <canvas id="stressTestChart" aria-label="Stress-test results as synthetic concurrent emote load increases"></canvas>
  </div>
  <p class="stress-chart__summary" aria-live="polite"></p>
  <noscript>Enable JavaScript to view the chart.</noscript>
</div>

## Methodology

Each point was measured with `/emote stress-test 30s N N`. `N-player equivalent` in the chart does not represent an actual player count. It means that both of the following load parameters were set to `N`.

| Value | Meaning in the implementation |
|-------|-------------------------------|
| Instances `N` | Creates `N` independent server-side emote playback instances and places their display entities in the world. Emotes are shuffled with a fixed seed and start at different playback positions. No additional `ServerPlayer` objects or player connections are created. |
| Packet fanout `N` | Passes each changed display metadata packet through `N` in-memory Minecraft packet encoders. This includes packet codec, compression, length framing, and generated byte-count costs, but excludes socket transmission, network latency, and client-side decoding and rendering. |

Therefore, `75-player equivalent` is a **synthetic load approximating 75 simultaneous emotes with every changed packet delivered to 75 recipients**. It models a case where all players are within the same tracking range and can see every emote, but it is not a test performed with 75 connected players. Actual fanout and load can be lower when players are spread apart or only some of them use emotes.

## Interpretation

- Average MSPT remained below the 50 ms threshold at 9.66 ms for 10-player equivalent, 13.33 ms for 25-player equivalent, and 26.93 ms for 50-player equivalent. It narrowly exceeded the threshold at 50.66 ms for 75-player equivalent and rose to 81.21 ms for 100-player equivalent. On this server and test configuration, the 75×75 load marks the point where sustaining 20 TPS becomes difficult.
- At 20 TPS, 600 ticks take 30 seconds. Total elapsed time remained at 30 seconds from 10- through 50-player equivalent, but increased to 32.0 seconds at 75-player equivalent and 51.4 seconds at 100-player equivalent, showing that the server could no longer maintain the target tick rate.
- As load increased, `Server/idle` decreased while `Network` rose from 1.0 to 41.3 seconds. At 100-player equivalent, `Network` accounted for about 80% of the 51.4-second total, indicating that change detection and packet serialization and compression were the primary bottleneck rather than emote timeline evaluation.
- These results cannot be used directly as the server's maximum player count. The actual limit depends on the proportion of players using emotes concurrently, player tracking ranges, displays per emote, server CPU, compression settings, other mods, and world load.
- The test system used 24 GB of memory in a single-channel configuration, so memory bandwidth may have affected the high-load results. Without a comparison against a dual-channel configuration, the size of that effect cannot be determined and memory bandwidth cannot be identified as the primary bottleneck.
